% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/borg_audit.R
\name{audit_predictions}
\alias{audit_predictions}
\title{Audit Predictions for Data Leakage}
\usage{
audit_predictions(
  predictions,
  train_idx,
  test_idx,
  actual = NULL,
  data = NULL,
  model = NULL
)
}
\arguments{
\item{predictions}{Vector of predictions (numeric or factor).}

\item{train_idx}{Integer vector of training indices.}

\item{test_idx}{Integer vector of test indices.}

\item{actual}{Optional vector of actual values for comparison.}

\item{data}{Optional data frame containing the original data.}

\item{model}{Optional fitted model object for additional checks.}
}
\value{
A BorgRisk object with audit results.
}
\description{
Validates that predictions were generated correctly without data leakage.
Checks that predictions correspond to test data only and that the
prediction process did not use information from the test set.
}
\examples{
# Create data and split
set.seed(42)
data <- data.frame(y = rnorm(100), x = rnorm(100))
train_idx <- 1:70
test_idx <- 71:100

# Fit model and predict
model <- lm(y ~ x, data = data[train_idx, ])
predictions <- predict(model, newdata = data[test_idx, ])

# Audit predictions
result <- audit_predictions(predictions, train_idx, test_idx)

}
