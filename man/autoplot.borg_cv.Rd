% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/borg_autoplot.R
\name{autoplot.borg_cv}
\alias{autoplot.borg_cv}
\title{Autoplot Method for borg_cv Objects}
\usage{
\method{autoplot}{borg_cv}(
  object,
  type = c("folds", "spatial", "sizes"),
  data = NULL,
  coords = NULL,
  raster = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{borg_cv} object from \code{borg_cv()}.}

\item{type}{Character. Plot type: \code{"folds"} (default), \code{"spatial"},
or \code{"sizes"}.}

\item{data}{Optional data frame (or sf/SpatVector) for spatial plots.
Required for \code{type = "spatial"}.}

\item{coords}{Character vector of coordinate column names. Required for
\code{type = "spatial"} when \code{data} is a plain data.frame.}

\item{raster}{Optional \code{terra::SpatRaster}. When provided with
\code{type = "spatial"}, the first layer is rendered as a background
map behind the fold points. Requires the \pkg{tidyterra} package.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{ggplot} object.
}
\description{
Visualizes cross-validation fold structure.
}
\details{
Requires the \pkg{ggplot2} package.
}
\examples{
\donttest{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  set.seed(42)
  d <- data.frame(x = runif(100), y = runif(100), z = rnorm(100))
  cv <- borg_cv(d, coords = c("x", "y"), target = "z")
  ggplot2::autoplot(cv, type = "sizes")
  ggplot2::autoplot(cv, type = "spatial", data = d, coords = c("x", "y"))
}
}

}
