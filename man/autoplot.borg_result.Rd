% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/borg_autoplot.R
\name{autoplot.borg_result}
\alias{autoplot.borg_result}
\title{Autoplot Method for borg_result Objects}
\usage{
\method{autoplot}{borg_result}(
  object,
  type = c("split", "spatial", "temporal", "groups"),
  fold = 1,
  data = NULL,
  coords = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{borg_result} object from \code{borg()}.}

\item{type}{Character. Plot type: \code{"split"} (default), \code{"spatial"},
\code{"temporal"}, or \code{"groups"}.}

\item{fold}{Integer. Which fold to plot. Default: 1.}

\item{data}{Optional data frame (or sf/SpatVector) for spatial plots.
Required for \code{type = "spatial"} to obtain coordinates.}

\item{coords}{Character vector of coordinate column names. Required for
\code{type = "spatial"} when \code{data} is a plain data.frame.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{ggplot} object.
}
\description{
Creates ggplot2 visualizations of BORG diagnosis + CV results.
}
\details{
Requires the \pkg{ggplot2} package. For spatial plots, the
\pkg{sf} package is recommended for proper map projections.
}
\examples{
\donttest{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  set.seed(42)
  d <- data.frame(x = runif(100), y = runif(100), z = rnorm(100))
  result <- borg(d, coords = c("x", "y"), target = "z")
  ggplot2::autoplot(result)
  ggplot2::autoplot(result, type = "spatial", data = d, coords = c("x", "y"))
}
}

}
