% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/borg_compare_models.R
\name{borg_compare_models}
\alias{borg_compare_models}
\title{Compare Multiple Models with Spatial CV}
\usage{
borg_compare_models(
  data,
  folds,
  models,
  metric = c("rmse", "mae", "rsq", "auc", "tss", "kappa"),
  fit_fun = stats::lm
)
}
\arguments{
\item{data}{Data frame.}

\item{folds}{A \code{borg_cv} object or fold list.}

\item{models}{Named list of either:
\itemize{
\item Formulas: \code{list(lm = y ~ x, full = y ~ x + z)}
\item Fitted models: \code{list(lm = lm_fit, rf = rf_fit)}
}}

\item{metric}{Character. Default: \code{"rmse"}.}

\item{fit_fun}{Function. Used when \code{models} contains formulas.
Default: \code{lm}.}
}
\value{
A data frame with class \code{"borg_model_comparison"} containing
model name, mean metric, SD, and rank. Has \code{autoplot()} method.
}
\description{
Evaluates multiple models (or formulas) using the same blocked CV
folds, producing a side-by-side comparison table.
}
\examples{
set.seed(42)
d <- data.frame(x = runif(100), y = runif(100),
                 a = rnorm(100), b = rnorm(100))
d$z <- d$a * 2 + rnorm(100, sd = 0.5)
cv <- borg_cv(d, coords = c("x", "y"), target = "z")
comp <- borg_compare_models(d, cv,
  models = list(simple = z ~ a, full = z ~ a + b))
comp

}
