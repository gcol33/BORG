% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/borg_fold_performance.R
\name{borg_fold_performance}
\alias{borg_fold_performance}
\title{Evaluate Per-Fold Model Performance}
\usage{
borg_fold_performance(
  data,
  folds,
  formula,
  coords = NULL,
  metric = c("rmse", "mae", "rsq", "auc", "tss", "kappa", "sensitivity", "specificity",
    "accuracy"),
  fit_fun = stats::lm,
  parallel = FALSE
)
}
\arguments{
\item{data}{Data frame with predictor and target columns.}

\item{folds}{A \code{borg_cv} object, or a list of fold lists
(each with \code{$train} and \code{$test} index vectors).}

\item{formula}{A model formula (e.g. \code{y ~ x1 + x2}).}

\item{coords}{Character vector of length 2. Coordinate column names
for computing fold centroids. Optional.}

\item{metric}{Character. Performance metric: \code{"rmse"} (default),
\code{"mae"}, or \code{"rsq"}.}

\item{fit_fun}{Function to fit a model. Default: \code{lm}. Must accept
\code{formula} and \code{data} arguments and return an object with a
\code{predict()} method.}

\item{parallel}{Logical. If \code{TRUE} and the \pkg{future.apply} package
is installed, fold evaluation runs in parallel. Default: \code{FALSE}.}
}
\value{
A data frame with columns:
\describe{
\item{fold}{Fold index}
\item{metric}{Metric name}
\item{value}{Metric value}
\item{n_train}{Training set size}
\item{n_test}{Test set size}
\item{centroid_x, centroid_y}{Spatial centroid of test set (if coords provided)}
}
Has class \code{"borg_fold_perf"} with an \code{autoplot()} method.
}
\description{
Fits a model on each fold's training set and evaluates on its test set,
returning per-fold metrics with spatial centroids for geographic
performance mapping.
}
\examples{
set.seed(42)
d <- data.frame(
  x = runif(200, 0, 100), y = runif(200, 0, 100),
  z = rnorm(200)
)
cv <- borg_cv(d, coords = c("x", "y"), target = "z")
perf <- borg_fold_performance(d, cv, z ~ x + y, coords = c("x", "y"))
perf

}
