% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/borg_ffs.R
\name{borg_forward_selection}
\alias{borg_forward_selection}
\title{Forward Feature Selection with Spatial/Blocked CV}
\usage{
borg_forward_selection(
  data,
  target,
  predictors = NULL,
  folds = NULL,
  coords = NULL,
  time = NULL,
  groups = NULL,
  metric = c("rmse", "mae", "rsq"),
  fit_fun = stats::lm,
  min_vars = 1L,
  verbose = FALSE
)
}
\arguments{
\item{data}{Data frame.}

\item{target}{Character. Response variable name.}

\item{predictors}{Character vector. Candidate predictor names. If
\code{NULL}, uses all numeric columns except target and coords.}

\item{folds}{A \code{borg_cv} object or fold list. If \code{NULL},
generated automatically via \code{borg_cv()}.}

\item{coords}{Character vector of coordinate columns (for auto CV).}

\item{time}{Character. Time column (for auto CV).}

\item{groups}{Character. Group column (for auto CV).}

\item{metric}{Character. \code{"rmse"} (default), \code{"mae"}, or
\code{"rsq"}.}

\item{fit_fun}{Function. Model fitting function. Default: \code{lm}.}

\item{min_vars}{Integer. Minimum variables to include. Default: 1.}

\item{verbose}{Logical. Default: FALSE.}
}
\value{
A list with class \code{"borg_ffs"} containing:
\describe{
\item{selected}{Character vector of selected variable names (in order)}
\item{history}{Data frame: step, variable_added, metric_value, n_vars}
\item{best_metric}{Best CV metric achieved}
\item{all_vars}{All candidate variables}
}
}
\description{
Selects variables using blocked cross-validation instead of random CV,
avoiding overfitting to spatial/temporal structure. At each step, adds
the variable that most improves the CV metric.
}
\details{
Similar to CAST::ffs() but uses BORG's CV infrastructure and supports
any dependency structure (spatial, temporal, grouped).
}
\examples{
set.seed(42)
d <- data.frame(
  x = runif(100), y = runif(100),
  important = rnorm(100), noise1 = rnorm(100), noise2 = rnorm(100)
)
d$z <- d$important * 2 + rnorm(100, sd = 0.5)
ffs <- borg_forward_selection(d, target = "z",
                                predictors = c("important", "noise1", "noise2"),
                                coords = c("x", "y"))
ffs$selected

}
