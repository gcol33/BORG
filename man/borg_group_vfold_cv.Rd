% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/borg_wrappers.R
\name{borg_group_vfold_cv}
\alias{borg_group_vfold_cv}
\title{BORG-Guarded group_vfold_cv}
\usage{
borg_group_vfold_cv(
  data,
  group,
  v = NULL,
  balance = c("groups", "observations"),
  coords = NULL,
  time = NULL,
  target = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame.}

\item{group}{Character. Column name for grouping.}

\item{v}{Integer. Number of folds. Default: number of groups.}

\item{balance}{Character. How to balance folds: "groups" or "observations".}

\item{coords}{Character vector. Coordinate columns for spatial check.}

\item{time}{Character. Time column for temporal check.}

\item{target}{Character. Target variable for dependency detection.}

\item{...}{Additional arguments passed to \code{rsample::group_vfold_cv()}.}
}
\value{
An \code{rset} object from rsample.
}
\description{
A guarded version of \code{rsample::group_vfold_cv()} that validates
group-based CV is appropriate for the data structure.
}
\examples{
\dontrun{
# Clustered data - group CV is appropriate
data <- data.frame(
  site = rep(1:20, each = 5),
  x = rnorm(100),
  y = rnorm(100)
)
folds <- borg_group_vfold_cv(data, group = "site", v = 5)
}

}
