% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/borg_importance.R
\name{borg_importance}
\alias{borg_importance}
\title{Block-Permutation Variable Importance}
\usage{
borg_importance(
  model,
  data,
  target,
  coords = NULL,
  predictors = NULL,
  n_blocks = 10,
  n_rep = 10,
  metric = c("rmse", "mae"),
  seed = 42
)
}
\arguments{
\item{model}{A fitted model with a \code{predict()} method.}

\item{data}{Data frame with predictor columns.}

\item{target}{Character. Target variable name.}

\item{coords}{Character vector of length 2. Coordinate column names.
If \code{NULL}, falls back to standard row permutation.}

\item{predictors}{Character vector. Variables to assess. If \code{NULL},
uses all columns except target and coords.}

\item{n_blocks}{Integer. Number of spatial blocks for permutation.
Default: 10.}

\item{n_rep}{Integer. Number of permutation repeats per variable.
Default: 10.}

\item{metric}{Character. \code{"rmse"} (default) or \code{"mae"}.}

\item{seed}{Integer. Random seed. Default: 42.}
}
\value{
A data frame with class \code{"borg_importance"} containing:
\describe{
\item{variable}{Predictor name}
\item{importance}{Mean increase in error after permutation}
\item{importance_sd}{SD across repeats}
\item{rank}{Importance rank (1 = most important)}
}
Has an \code{autoplot()} method.
}
\description{
Computes permutation importance that respects spatial structure.
Instead of permuting individual rows (which breaks spatial
autocorrelation and inflates importance), permutes values
within spatial blocks.
}
\examples{
set.seed(42)
d <- data.frame(x = runif(100), y = runif(100),
                 a = rnorm(100), b = rnorm(100))
d$z <- d$a * 2 + rnorm(100, sd = 0.5)
model <- lm(z ~ a + b, data = d)
imp <- borg_importance(model, d, target = "z", coords = c("x", "y"))
imp

}
