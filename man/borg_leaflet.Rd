% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/borg_leaflet.R
\name{borg_leaflet}
\alias{borg_leaflet}
\title{Interactive Leaflet Map of Spatial CV Folds}
\usage{
borg_leaflet(object, data, coords, fold = 1)
}
\arguments{
\item{object}{A \code{borg_result} or \code{borg_cv} object with spatial folds.}

\item{data}{Data frame with coordinate columns. Required.}

\item{coords}{Character vector of length 2. Coordinate column names
(longitude first, latitude second).}

\item{fold}{Integer or \code{"all"}. Which fold to display. Default: 1.
Use \code{"all"} for a layer-group selector across all folds.}
}
\value{
A \code{leaflet} htmlwidget.
}
\description{
Creates an interactive leaflet map showing train/test point assignments
across CV folds. Requires geographic coordinates (lat/lon).
}
\details{
Requires the \pkg{leaflet} package. Coordinates must be in WGS84
(longitude/latitude). Points are color-coded: blue = train, red = test.
Click points for popups with index and fold assignment.
}
\examples{
\donttest{
if (requireNamespace("leaflet", quietly = TRUE)) {
  set.seed(42)
  d <- data.frame(
    lon = runif(100, 10, 20),
    lat = runif(100, 45, 55),
    z = rnorm(100)
  )
  result <- borg(d, coords = c("lon", "lat"), target = "z")
  borg_leaflet(result, data = d, coords = c("lon", "lat"))
}
}

}
