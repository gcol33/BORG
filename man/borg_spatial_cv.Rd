% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/borg_wrappers.R
\name{borg_spatial_cv}
\alias{borg_spatial_cv}
\title{Spatial Block Cross-Validation (rsample-compatible)}
\usage{
borg_spatial_cv(
  data,
  coords,
  target = NULL,
  v = 5,
  buffer = NULL,
  repeats = 1L,
  ...
)
}
\arguments{
\item{data}{A data frame, \code{sf} object, or \code{terra::SpatVector}.}

\item{coords}{Character vector of length 2. Coordinate column names.}

\item{target}{Character. Target variable for autocorrelation diagnosis.}

\item{v}{Integer. Number of folds. Default: 5.}

\item{buffer}{Numeric. Optional spatial buffer distance for exclusion.}

\item{repeats}{Integer. Number of repeated fold sets. Default: 1.}

\item{...}{Additional arguments passed to \code{\link{borg_cv}()}.}
}
\value{
A \code{borg_rset} object (subclass of \code{rset}) compatible
with tidymodels.
}
\description{
Creates spatial block CV folds that plug directly into
\code{tune::tune_grid()} and \code{tune::fit_resamples()}.
}
\examples{
\donttest{
if (requireNamespace("rsample", quietly = TRUE)) {
  set.seed(42)
  d <- data.frame(x = runif(100), y = runif(100), z = rnorm(100))
  folds <- borg_spatial_cv(d, coords = c("x", "y"), target = "z")
}
}

}
