% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/borg_wrappers.R
\name{borg_temporal_cv}
\alias{borg_temporal_cv}
\title{Temporal Block Cross-Validation (rsample-compatible)}
\usage{
borg_temporal_cv(data, time, target = NULL, v = 5, embargo = NULL, ...)
}
\arguments{
\item{data}{A data frame.}

\item{time}{Character. Time column name.}

\item{target}{Character. Target variable.}

\item{v}{Integer. Number of folds. Default: 5.}

\item{embargo}{Numeric. Time gap between train and test sets.}

\item{...}{Additional arguments passed to \code{\link{borg_cv}()}.}
}
\value{
A \code{borg_rset} object (subclass of \code{rset}) compatible
with tidymodels.
}
\description{
Creates temporal block CV folds that plug directly into
\code{tune::tune_grid()} and \code{tune::fit_resamples()}.
}
\examples{
\donttest{
if (requireNamespace("rsample", quietly = TRUE)) {
  d <- data.frame(time = 1:100, x = rnorm(100), y = cumsum(rnorm(100)))
  folds <- borg_temporal_cv(d, time = "time", target = "y")
}
}

}
