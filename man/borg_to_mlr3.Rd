% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/borg_mlr3_resampling.R
\name{borg_to_mlr3}
\alias{borg_to_mlr3}
\title{mlr3 Resampling for BORG Cross-Validation}
\usage{
borg_to_mlr3(data = NULL, folds = NULL, cv_obj = NULL, id = "borg")
}
\arguments{
\item{data}{A data frame. Required when using \code{folds} directly,
not needed when using \code{cv_obj}.}

\item{folds}{A list of lists, each with \code{train} and \code{test}
integer index vectors.}

\item{cv_obj}{A \code{borg_cv} object. If provided, \code{folds}
are extracted from it.}

\item{id}{Character. Resampling identifier. Default: \code{"borg"}.}
}
\value{
An \code{mlr3::Resampling} R6 object that can be used
directly with \code{mlr3::resample()}, \code{mlr3::benchmark()},
and other mlr3 infrastructure.
}
\description{
Creates a native \code{mlr3::Resampling} object from BORG
cross-validation folds, enabling direct use with mlr3 benchmarking
infrastructure.
}
\details{
The returned resampling is pre-instantiated — it contains fixed
train/test splits that respect BORG's spatial/temporal blocking.
Calling \code{$instantiate()} is not needed (and will not
overwrite the existing splits).
}
\examples{
\donttest{
if (requireNamespace("mlr3", quietly = TRUE)) {
  d <- data.frame(x = runif(100), y = runif(100), z = rnorm(100))
  cv <- borg_cv(d, coords = c("x", "y"), target = "z")
  resampling <- borg_to_mlr3(cv_obj = cv)
  resampling$iters  # number of folds
}
}

}
\seealso{
\code{\link{borg_cv}}, \code{\link{borg_rset}}
}
