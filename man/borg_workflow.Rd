% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/borg_workflow.R
\name{borg_workflow}
\alias{borg_workflow}
\title{Create a BORG Validation Workflow}
\usage{
borg_workflow(
  data,
  formula,
  coords = NULL,
  time = NULL,
  groups = NULL,
  v = 5,
  fit_fun = stats::lm,
  metric = c("rmse", "mae", "rsq"),
  buffer = NULL,
  parallel = FALSE,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{data}{Data frame with predictor and target columns.}

\item{formula}{Model formula (e.g. \code{y ~ x1 + x2}).}

\item{coords}{Character vector of coordinate column names.}

\item{time}{Character. Time column name.}

\item{groups}{Character. Group column name.}

\item{v}{Integer. Number of folds. Default: 5.}

\item{fit_fun}{Function to fit a model. Default: \code{lm}. Must accept
\code{formula} and \code{data} and return an object with \code{predict()}.}

\item{metric}{Character. Performance metric. Default: \code{"rmse"}.}

\item{buffer}{Numeric. Optional spatial buffer.}

\item{parallel}{Logical. If \code{TRUE} and \pkg{future.apply} is installed,
fold fitting and evaluation run in parallel. Default: \code{FALSE}.}

\item{verbose}{Logical. Default: FALSE.}

\item{...}{Additional arguments passed to \code{\link{borg_cv}()}.}
}
\value{
A \code{borg_workflow} object (list) containing:
\describe{
\item{diagnosis}{BorgDiagnosis object}
\item{cv}{borg_cv object with folds}
\item{models}{List of fitted models (one per fold)}
\item{predictions}{List of prediction vectors (one per fold)}
\item{performance}{borg_fold_perf data frame}
\item{risks}{List of BorgRisk objects (one per fold)}
\item{data}{Original data}
\item{formula}{Model formula}
\item{params}{Workflow parameters}
}
}
\description{
Wraps the full model evaluation pipeline into a single trackable object:
diagnose data dependencies, generate appropriate CV folds, fit a model
per fold, and validate each split for leakage.
}
\examples{
set.seed(42)
d <- data.frame(
  x = runif(100, 0, 100), y = runif(100, 0, 100),
  z = rnorm(100)
)
wf <- borg_workflow(d, z ~ x + y, coords = c("x", "y"))
wf

}
