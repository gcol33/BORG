% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/borg_plot.R
\name{plot.borg_result}
\alias{plot.borg_result}
\title{Plot BORG Result Objects}
\usage{
\method{plot}{borg_result}(
  x,
  type = c("split", "risk", "temporal", "groups"),
  fold = 1,
  time = NULL,
  groups = NULL,
  title = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{borg_result} object from \code{borg()}.}

\item{type}{Character. Plot type: \code{"split"} (default), \code{"risk"},
\code{"temporal"}, or \code{"groups"}.}

\item{fold}{Integer. Which fold to plot (for split visualization). Default: 1.}

\item{time}{Column name or values for temporal plots.}

\item{groups}{Column name or values for group plots.}

\item{title}{Optional custom plot title.}

\item{...}{Additional arguments passed to internal plot functions.}
}
\value{
Invisibly returns NULL. Called for plotting side effect.
}
\description{
S3 plot method for borg_result objects from \code{borg()}.
}
\examples{
set.seed(42)
data <- data.frame(
  x = runif(100, 0, 100),
  y = runif(100, 0, 100),
  response = rnorm(100)
)
result <- borg(data, coords = c("x", "y"), target = "response")
plot(result)  # Split visualization for first fold

}
